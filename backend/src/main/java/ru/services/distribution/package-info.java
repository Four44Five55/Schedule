/**
 * Двухфазная раскладка занятий: фаза 1 — лекции, фаза 2 — практики и цепочки.
 *
 * <p>{@code DistributionDiscipline} — оркестратор (~113 строк после рефакторинга 2026-02-26, до него
 * God Class на 1740); общее состояние прохода живёт в {@code DistributionContext}, поиск даты — за
 * {@code DateFinder} (<b>Strategy</b>, реализация — скользящее окно), проверки — в
 * {@code PlacementValidator}, подбор пары для практики — за {@code PracticeSlotStrategy}
 * (Strategy + Selector: стандартный режим и эскалация при нехватке дней у преподавателя).</p>
 *
 * <p><b>Порядок преподавателей — часть алгоритма:</b> {@code EducatorPrioritizer} сортирует по числу
 * групп в лекциях, затем по общему числу занятий. Кто раскладывается раньше, тот получает лучшие
 * места, поэтому порядок здесь — не деталь реализации.</p>
 *
 * <p><b>Чего пакет НЕ делает.</b> Он не улучшает готовое расписание: на насыщенной сетке
 * компактность не создаётся, а перераспределяется между людьми, и локальная оптимизация «после»
 * только переносит окна с одного преподавателя на другого. Замысел конструктивной генерации и
 * запрет трогать фазу 3 вслепую (сетки-теста под генерацию нет) — в
 * {@code docs/NORTH_STAR_FAIR_GENERATION.md}.</p>
 */
package ru.services.distribution;
