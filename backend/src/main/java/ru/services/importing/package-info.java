/**
 * Импорт расписания из выгрузки сторонней программы: разбор HTML-полотна → сверка справочников →
 * заведение недостающего → склейка разрезов → расчёт учебного плана → запись и откат.
 *
 * <p><b>Порядок шагов не случаен</b> и закреплён решениями разбора с заказчиком (реестр И-1…И-31 —
 * в {@code docs/IMPORT_FORMAT.md} §11): подразделения заводятся первыми (И-26), потому что без них
 * зависимые записи молча остаются без привязки; план пишется после {@code create-missing}, потому
 * что занятие не разрешается без дисциплины, группы и потока.</p>
 *
 * <p><b>Что чему верит.</b> Источник истины по позиции занятия — групповой разрез (И-1);
 * преподавательский разрез отвечает на вопрос «кто ведёт» и показывает то, что групповой теряет
 * маркерами и границами периода (И-21); аудиторный разрез даёт кафедру-владельца комнаты (И-24).
 * Подвал группового файла — перечень кандидатов, а не свидетельство о занятии (И-23).
 * Кластер параллельных занятий — не занятие: они разводятся по физическому пересечению (И-29).</p>
 *
 * <p><b>Границы.</b> Пакет ничего не решает молча: всё, что разбор не вывел, приезжает в отчёт
 * ({@code ImportReport}, {@code ImportMatchReport}, {@code PlanReport}) и уточняется человеком до
 * записи. Запись идёт в новую сессию через {@code ScheduleSessionGate} и откатывается целиком
 * ({@code ImportRollbackService}).</p>
 */
package ru.services.importing;
