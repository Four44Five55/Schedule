/**
 * Кто строит снимок планирования и кто его хранит.
 *
 * <p>{@code WorkspaceProvider} — «одолжить workspace на время операции» (интерфейс),
 * {@code RebuildingWorkspaceProvider} — прежнее поведение «строить заново»,
 * {@code CachingWorkspaceProvider} — <b>Decorator</b> над ним ({@code @Primary}) с ключом
 * {@code (sessionId, version)} плюс поколение ограничений и срок годности: версия сессии знает не
 * про всё, что делает снимок неактуальным.</p>
 *
 * <p><b>Почему отдельный пакет, а не поле в сервисе:</b> построение снимка и его хранение — разные
 * ответственности, и выключение кэша должно быть неподключённым декоратором (OCP), а не флагом
 * внутри {@code WorkspaceRecreationService}. Читатели зависят от интерфейса (DIP).</p>
 *
 * <p><b>Только читающие пути.</b> Мутаторы строят снимок сами: им нужен свежий авторитетный
 * экземпляр, который они необратимо меняют. Читатель, которому надо «изъять занятие и посмотреть»,
 * обязан пользоваться {@code ScheduleWorkspace.withoutPlacements(...)} — возврат гарантируется
 * {@code finally}, иначе испорченный общий снимок переживёт запрос.</p>
 */
package ru.services.workspace;
