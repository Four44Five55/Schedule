/**
 * Пространство планирования: сетка расписания, занятость ресурсов и подбор места для занятия.
 *
 * <p>{@code ScheduleWorkspace} — рабочий снимок в памяти: {@code ScheduleGrid} (какие ячейки заняты)
 * + {@code ResourceAvailabilityManager} (кто когда свободен). Размещение атомарно:
 * {@code executePlacement}/{@code removePlacement} меняют сетку и занятость вместе, чтобы снимок не
 * побывал в состоянии «в сетке есть, в занятости нет».</p>
 *
 * <p><b>Календарь — значение, а не статика.</b> Какие ячейки (дата · пара) вообще существуют,
 * говорит {@code AcademicCalendar}, выведенный из периода сессии. До 2026-08-27 на этот вопрос
 * отвечала статическая карта на всю JVM — единственное изменяемое глобальное состояние в
 * {@code src/main} и источник гонок «двое смотрят разные периоды».</p>
 *
 * <p><b>«Изъять → поработать → вернуть».</b> Читателю, которому надо посмотреть расписание без
 * какого-то занятия, полагается {@code withoutPlacements(lessons, body)}: возврат в {@code finally},
 * в обратном порядке, с запомненными ячейкой и аудиториями. Возврат «последней строкой» не годится —
 * с общим кэшированным снимком испорченный объект переживает запрос и теряет занятие без единой
 * ошибки в логе.</p>
 *
 * <p>Кто строит и хранит снимок — в {@link ru.services.workspace}.</p>
 */
package ru.services.solver;
