/**
 * Пересортировка трека в порядок плана после переноса — и подбор комнат для сдвинутых занятий.
 *
 * <p><b>Правило:</b> занятия класса идут в порядке изучения ({@code planPosition}). Перенос ломает
 * порядок, {@code TrackReorderStrategy} его восстанавливает «пузырьком»: i-я по времени ячейка
 * получает i-е по плану занятие, а те, через кого перенесённое перепрыгнуло, сдвигаются на одну
 * ячейку. Меняются только даты и пары — содержание занятия едет с ним.</p>
 *
 * <p><b>Чистые функции без БД.</b> Вход собирает вызывающий слой ({@code TimedLesson},
 * {@code ReorderRoomInput}), пакет возвращает план ({@code ReorderPlan}, {@code ReorderRoomPlan}) и
 * находки ({@code ReorderProblem}) — применяет их транзакция переноса, в которую пересортировка
 * входит неразделимо: между «перенесли» и «пересортировали» не должно быть окна.</p>
 *
 * <p><b>Предусловие:</b> на вход подаётся ОДИН однородный класс (поток + состав преподавателей),
 * (дата, пара) внутри него уникальны. Разбиение по идентичности — на сборочном слое.</p>
 */
package ru.services.reindex;
