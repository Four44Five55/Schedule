package ru.services.reindex;

/**
 * Занятость комнаты в конкретной ячейке — ключ проверки «свободна ли комната здесь».
 *
 * <p>Единица, из которой складывается снимок занятости <b>не-классных</b> размещений при
 * пересортировке: сами занятия класса переставляются по своим же ячейкам (всегда разным —
 * перестановка биективна), поэтому столкнуться комната жёсткого занятия может только с тем,
 * кто в класс не входит и с места не двигается. Снимок собирается в памяти, workspace не нужен.</p>
 *
 * @param roomId аудитория
 * @param cell   ячейка (дата × пара)
 */
public record RoomSlot(Integer roomId, Cell cell) {
}
