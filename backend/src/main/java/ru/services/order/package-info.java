/**
 * Порядок изучения: занятие не должно стоять раньше предшествующей ему по плану лекции
 * ({@code BEFORE_LECTURE}) и не должно отрываться от неё дальше нормы ({@code FAR_FROM_LECTURE},
 * порог — {@code schedule.order.max-lecture-gap-days}).
 *
 * <p><b>Подсказка, а не запрет:</b> находки подсвечивают ячейки, но не фильтруют их и не блокируют
 * перенос. Решение остаётся за диспетчером.</p>
 *
 * <p><b>Шаг 1 из задуманного.</b> Правило сознательно простое: сравнивается только с последней
 * предшествующей лекцией курса, в пределах дорожки группы. Три более общие формулировки были
 * опробованы и откачены — разбор тупиков и требования к любой будущей версии в
 * {@code docs/ORDER_HIGHLIGHT_ROLLBACK.md}; идти дальше надо оттуда, а не изобретать заново.</p>
 *
 * <p>{@code LessonOrderRule} — чистая функция с юнит-тестами; БД и Spring в пакет не входят.</p>
 */
package ru.services.order;
