package ru.services.stream;

import java.util.UUID;

/**
 * «Звонок» клиенту: расписание сессии изменилось и уже спроецировано.
 *
 * <p>Намеренно тонкий: ни занятий, ни дат. Клиент, увидев поколение выше своего, перечитывает
 * данные обычными REST-эндпоинтами и подхватывает версию. Возить содержимое ещё и по этому каналу —
 * значит завести вторую доставку данных, которая рано или поздно разойдётся с первой.</p>
 *
 * @param sessionId сессия расписания
 * @param version   актуальное поколение агрегата (то же, что в {@code ScheduleSessionDto.version})
 */
public record ScheduleChangedDto(UUID sessionId, Long version) {
}
