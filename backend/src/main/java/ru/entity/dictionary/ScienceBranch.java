package ru.entity.dictionary;

import jakarta.persistence.Entity;
import jakarta.persistence.Table;

/**
 * Отрасль науки учёной степени: «технические», «физико-математические».
 *
 * <p>Вторая половина степени: уровень (кандидат/доктор) — {@link ru.enums.AcademicDegree},
 * отрасль — здесь. Готовое сокращение «ктн» не хранится нигде: оно собирается из уровня и
 * отрасли, иначе появилось бы третье представление одного факта.</p>
 *
 * <p>{@code shortName} — буквенная часть: «т» даёт «ктн», «фм» даёт «кфмн». Точек нет ни в
 * данных, ни в собранной подписи (уточнено 2026-08-14): конвенция взята из документов, откуда мы
 * читаем данные, поэтому наша подпись и подпись из чужой выгрузки сравнимы напрямую.</p>
 */
@Entity
@Table(name = "science_branch")
public class ScienceBranch extends AbstractEducatorDictionary {
}
