package ru.dto.command;

/**
 * Итог очистки размещений: сколько снято + сессия с АКТУАЛЬНОЙ версией.
 *
 * <p>Раньше эндпоинт отдавал голое число. Пока версия не росла при очистке, это было незаметно;
 * теперь очистка поднимает поколение агрегата, и клиент обязан подхватить новую версию — иначе
 * его следующая команда придёт с устаревшей и получит 409.</p>
 *
 * @param removed сколько размещений удалено (закреплённые не трогаются)
 * @param session сессия-владелец с новой {@code version}
 */
public record ClearPlacementsResponse(
    int removed,
    ScheduleSessionDto session
) {
}
